### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Mutation {
  correctRecord(date: String!, login: String!, value: Int!): Boolean!
  createProfile(adminRole: Boolean!, email: String!, login: String!, name: String!, phone: String!, position: String!): Boolean!
  deleteProfile(login: String!): Boolean!
  setRecord(login: String!, process: String!): Boolean!
  updateProfile(adminRole: Boolean!, email: String!, login: String!, name: String!, phone: String!, position: String!): Boolean!
}

type Profile {
  adminRole: Boolean!
  email: String!
  login: String!
  name: String!
  phone: String!
  position: String!
}

type Query {
  dailyRecords: [Record]
  loadRecordForSet(login: String!): RecordWOpt
  login(login: String!): Boolean!
  monthlyRecords(date: String!): [Record]
  recordsIssues: RecordsIssues
  userList: [Profile]
}

type Record {
  brk_hrs: Int
  cfbreak: Int
  date: String!
  end: String
  login: String!
  name: String
  start: String
  status: Boolean!
  wrk_hrs: Int
}

type RecordWOpt {
  adminRole: Boolean!
  options: String!
  recordData: Record!
}

type RecordsIssues {
  fix_required: [Record]
  suspicious: [Record]
}